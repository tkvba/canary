kind: pipeline
name: default

steps:
- name: build
  image: python
  commands:
  - echo "inside container"
  
- name: publish
  image: plugins/docker
  settings:
    username: sndjnky
    password: 
      from_secret: p_docker
    repo: sndjnky/tk
    tag:
    - canary_v11
  when:
    event:
    - push
