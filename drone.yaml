kind: pipeline
name: default

steps:
- name: build
  image: nginx:latest
  settings:
    dry_run: false
    repo: sndjnky/tk
  when:
    event:
    - pull_request
    - push

- name: push
  image: plugins/docker
  settings:
    username: sndjnky
    password: 
      from_secret: p_docker
    repo: sndjnky/tk
    tag:
    - canary_v4
  when:
    event:
    - custom
