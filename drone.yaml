kind: pipeline
name: default

steps:
- name: build
  image: nginx
  commands:
  - echo "inside container"
  
- name: publish
  image: plugins/docker
  settings:
    username: sndjnky
    password: 
      from_secret: p_docker
    repo: sndjnky/tk
    tag:
    - canary_v15
  when:
    event:
    - push
