kind: pipeline
name: default

steps:
- name: build
  image: python
  commands:
  - pip install -r requirements.txt
- name: push
  image: plugins/docker
  settings:
    username: sndjnky
    password: 
      from_secret: p_docker
    repo: sndjnky/tk
    tag:
    - auto_tag: true
  when:
    event:
    - custom
